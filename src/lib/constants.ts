/** จำนวนสินค้าที่ถือว่า "ใกล้หมด" (Low Stock) */
export const LOW_STOCK_THRESHOLD = 50;

/** Prefix สำหรับ productId เช่น INV001 */
export const PRODUCT_ID_PREFIX = "INV";

/** จำนวนหลักของตัวเลขใน productId */
export const PRODUCT_ID_PAD_LENGTH = 3;